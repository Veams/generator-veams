{
    "name": "<%= _.slugify(projectName) %>",
    "description": "Prototype",
    "version": "0.1.0",
    "author": {
        "name": "<%= _.slugify(projectAuthor) %>",
        "email": "<%= authorEmail %>",
        "url": "http://github.com/<%= authorLogin %>/"
    },
    "engines": {
        "node": ">=0.8"
    },
    "scripts": {
        "test": "grunt test"
    },
    "devDependencies": {
	"grunt": "~0.4.1", <% if(installAssemble === true){ %>
        "assemble": "~0.4.12", <% } %>
        "grunt-newer": "~0.5.4",
        "grunt-contrib-clean": "~0.5.0",
		"grunt-bg-shell": "*",
        "grunt-contrib-connect": "~0.5.0",
        "grunt-contrib-watch": "~0.5.3",
        "load-grunt-tasks": "~0.1.3",
        "grunt-timer": "~0.1.0"<% if(plugin && plugin.length > 0){ %>,
<% if(typeof plugin === 'object'){ _.each(plugin, function(name, i) { %>
    "<%= name %>": "*"<% if(i < (plugin.length - 1)) { %>,<% } %>
<% }); } else { %>
    "<%= plugin %>": "*"
    <%} } %><% if(modules && modules.length > 0){ %>,
<% if(typeof modules === 'object'){ _.each(modules, function(name, i) { %>
    "<%= name %>": "*"<% if(i < (modules.length - 1)) { %>,<% } %>
<% }); } else { %>
    "<%= modules %>": "*"
    <%} } %>
},
"licenses": [
    {
      "type": "MIT",
      "url": "https://github.com/<%= authorLogin %>/<%= _.slugify(projectName) %>/blob/master/LICENSE-MIT"
    }
  ]
}
