define(["jquery", "backbone"<% if(temp === true){ %>, "text!templates/<%= path %><%= initName %>.html"<% } %>],

	function($, Backbone<% if(temp === true){ %>,template<% } %>){

	var <%= initName %> = Backbone.View.extend({
			// The DOM Element associated with this view
			el: ".yourElement",

			// View Event Handlers
			events: {
			},

			// View constructor
			initialize: function() {
				// Calls the view's render method
				// Please remove render() form initialize() and execute it when it is needed
				this.render();
			},

			// Renders the view's template to the UI
			render: function() {
				<% if(temp === true){ %>
				// Setting the view's template property using the Underscore template method
				this.template = _.template(template, {});

				// Dynamically updates the UI with the view's template
				this.$el.html(this.template);
				<% } %>
				// Maintains chainability
				return this;
			}
		});
		// Returns the View class
		return <%= initName %>;
	}
);
