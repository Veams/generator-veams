module.exports = {
	options: {
		notify: false,
		host: "localhost",
		proxy: 'localhost:<%%= ports.app %>',
		watchTask: true,
		ghostMode: {
			clicks: true,
			scroll: true,
			forms: true
		}
	},
	bsFiles: {
		src: [
			'<%%= paths.dev %>/css/styles.css', <% if (docs && docs.length > 0 && docs.indexOf('htmldoc') != -1) { %>
			'<%%= paths.dev %>/css/docs.css', <% } %>
			'<%%= paths.dev %>/js/**/*.js',
			'<%%= paths.dev %>/**/*.html'
		]
	}
};