module.exports = {
	bsFiles: {
		src: [
			'<%%= paths.dev %>/css/styles.css', <% if (docs && docs.length > 0 && docs.indexOf('htmldoc') != -1) { %>
			'<%%= paths.dev %>/css/docs.css', <% } %>
			'<%%= paths.dev %>/js/**/*.js',
			'<%%= paths.dev %>/**/*.html'
		]
	},
	options: {
		host: "localhost",
		server: {
			baseDir: '<%%= paths.dev %>',
			index: "index.html"
		},
		watchTask: true,
		ghostMode: {
			clicks: true,
			scroll: true,
			links: true,
			forms: true
		}
	}
};