module.exports = {
    syncing: {
        tasks: [
            'sync'
        ],
        options: {
            logConcurrentOutput: true
        }
    },
    build: {
        tasks: [
			'jshint',
            'htmlhint'<% if (gruntModules && gruntModules.length) { if (gruntModules.indexOf('grunt-contrib-htmlmin') != -1) { %>,
            'htmlmin'<% }} %><% if (gruntModules && gruntModules.length) { if (gruntModules.indexOf('grunt-jsdoc') != -1) { %>,
            'jsdoc'<% }} %>
        ],
        options: {
            logConcurrentOutput: true,
            limit: 5
        }
	}
};