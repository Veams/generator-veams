<% if (cssLibs && cssLibs.length > 0 && cssLibs.indexOf('neat') != -1) { %>@import "../../bower-components/neat/app/assets/stylesheets/neat-helpers";
/* ===================================================
BOURBON NEAT
=================================================== */
$column: 73px;
$gutter: 22px;
$grid-columns: 12;
$max-width: 1138px;

// Define your breakpoints for Bourbon Neat
$bp-tablet-l: new-breakpoint(max-width 1024px 12);
$bp-tablet-p: new-breakpoint(max-width 768px 8);
$bp-mobile-l: new-breakpoint(max-width 640px 8);
$bp-mobile-p: new-breakpoint(max-width 480px 4);
<% } else { %>
// Demo vars, when you use bourbon neat just delete them and comment out the two lines above
$bp-tablet-l: 1024px;
$bp-tablet-p: 768px;
$bp-mobile-l: 640px;
$bp-mobile-p: 480px;
<% } %>
<% if (veamsPackages && veamsPackages.length && veamsPackages.indexOf('veamsSCSS') != -1) { %>
$use-min-width: false; // This variable will be used veams-scss
<% } %>
<% if (cssLibs && cssLibs.length > 0 && cssLibs.indexOf('foundation') != -1) { %>
/* ===================================================
FOUNDATION SETTINGS
=================================================== */
// The settings can be found here:
// ../../bower-components/foundation/scss/foundation/_settings.scss
// Just copy and paste the things you need
<% } %>
<% if (cssLibs && cssLibs.length > 0 && cssLibs.indexOf('bootstrap-sass') != -1) { %>
/* ===================================================
BOOTSTRAP SETTINGS
=================================================== */
// The settings can be found here:
// ../../bower-components/bootstrap-sass/assets/stylesheets/bootstrap/_variables.scss
// Just copy and paste the things you need

<% } %>