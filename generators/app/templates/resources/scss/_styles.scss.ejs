<% if (veamsPackages && veamsPackages.length && veamsPackages.indexOf('veamsSCSS') != -1) { %>
// Reset your styles via reset or normalize
@import "../bower-components/veams-scss/resources/scss/veams-reset";<% } %>

// Global Variables and Settings
@import "global/_vars";<% if (veamsPackages && veamsPackages.length && veamsPackages.indexOf('veamsSCSS') != -1) { %>
@import "../bower-components/veams-scss/resources/scss/veams";<% } %>
<% if (cssLibs && cssLibs.length) { if (cssLibs.indexOf('foundation') != -1) { %>
// Frameworks
@import "../bower-components/foundation/scss/foundation"; // You can choose your specific components<% }} %> <% if (cssLibs && cssLibs.length) { if (cssLibs.indexOf('neat') != -1) { %>
@import "../bower-components/bourbon/app/assets/stylesheets/bourbon";
@import "../bower-components/neat/app/assets/stylesheets/neat";<% }} %> <% if (cssLibs && cssLibs.length) { if (cssLibs.indexOf('sass-bootstrap') != -1) { %>
@import "../bower-components/sass-bootstrap/lib/bootstrap"; // You can choose your specific components<% }} %>

// Icons
@import "icons/**/*";

// Mixins
@import "utils/mixins/**/*";

// Extends
@import "utils/extends/**/*";

// Globals
@import "global/_base";<% if (veamsPackages && veamsPackages.length && veamsPackages.indexOf('veamsMethodology') != -1) { %>

// Regions
@import "regions/**/*";

// Components
@import "components/**/*";

// Blocks
@import "blocks/**/*";<% } %>

// Print
@import "global/_print";