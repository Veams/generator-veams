/* ===================================================
Main File For Imports
=================================================== */
<% if (cssLibs && cssLibs.length) { if (cssLibs.indexOf('neat') != -1) { %>
// Helpers
@import "../bower-components/bourbon/app/assets/stylesheets/bourbon";<% }} %>
<% if (veamsPackages) { %>
// Reset (veams-reset or veams-normalize)
@import "../../node_modules/veams/src/scss/veams-reset";<% } %>

// Global Variables and Settings
@import "global/_vars";<% if (veamsPackages) { %>
@import "../../node_modules/veams/src/scss/veams";<% } %><% if (cssLibs && cssLibs.length) { %>

// Frameworks <% if (cssLibs.indexOf('include-media') != -1) { %>
@import "../bower-components/include-media/dist/include-media";<% } %><% if (cssLibs.indexOf('foundation') != -1) { %>
@import "../bower-components/foundation/scss/foundation"; // You can choose your specific components<% } %><% if (cssLibs.indexOf('neat') != -1) { %>
@import "../bower-components/neat/core/neat";<% } %> <% if (cssLibs.indexOf('bootstrap-sass') != -1) { %>
@import "../bower-components/bootstrap-sass/assets/stylesheets/bootstrap"; // You can choose your specific components<% }} %>

// Icons
@import "icons/**/*";

// Helpers
@import "helpers/functions/**/*";
@import "helpers/mixins/**/*";

// Globals
@import "global/_base";<% if (veamsPackages) { %>
@import "global/_get-media";

// Regions in Layouts
@import "layouts/**/*";
<% if (!selfContained) { %>
// Components
@import "components/**/*";

// Blocks
@import "blocks/**/*";

// Utilities
@import "utilities/**/*";<% } %><% } %>

<% if (selfContained) { %>
// @INSERTPOINT :: @ref: scss-self-contained-import, @keep: true //<% } %>

// Print
@import "global/_print";