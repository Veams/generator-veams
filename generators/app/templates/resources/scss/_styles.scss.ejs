/* ===================================================
Main File For Imports
=================================================== */
<% if (cssLibs && cssLibs.length) { if (cssLibs.indexOf('neat') != -1) { %>
// Helpers
@import "../bower-components/bourbon/app/assets/stylesheets/bourbon";<% }} %>
<% if (veamsPackages && veamsPackages.length && veamsPackages.indexOf('veamsSCSS') != -1) { %>
// Reset (veams-reset or veams-normalize)
@import "../bower-components/veams-sass/resources/scss/veams-reset";<% } %>

// Global Variables and Settings
@import "global/_vars";<% if (veamsPackages && veamsPackages.length && veamsPackages.indexOf('veamsSCSS') != -1) { %>
@import "../bower-components/veams-sass/resources/scss/veams";<% } %>
<% if (cssLibs && cssLibs.length) { if (cssLibs.indexOf('foundation') != -1) { %>
// Frameworks
@import "../bower-components/foundation/scss/foundation"; // You can choose your specific components<% } %> <% if (cssLibs.indexOf('neat') != -1) { %>
@import "../bower-components/neat/app/assets/stylesheets/neat";<% } %> <% if (cssLibs.indexOf('bootstrap-sass') != -1) { %>
@import "../bower-components/bootstrap-sass/assets/stylesheets/bootstrap"; // You can choose your specific components<% }} %>

// Icons
@import "icons/**/*";

// Mixins
@import "utils/mixins/**/*";

// Extends
@import "utils/extends/**/*";

// Globals
@import "global/_base";<% if (veamsPackages && veamsPackages.length && veamsPackages.indexOf('veamsJS') != -1) { %>
// @INSERTPOINT :: @ref: veamsJS-scss-import, @keep: false <% } %>
<% if (veamsPackages && veamsPackages.length && veamsPackages.indexOf('veamsMethodology') != -1) { %>
// Regions in Layouts
@import "layouts/**/*";

// Components
@import "components/**/*";

// Blocks
@import "blocks/**/*";<% } %>

// Print
@import "global/_print";