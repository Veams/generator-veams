// Global dependencies <%if (jsLibs && jsLibs.indexOf('jquery') !== -1) { %>
var $ = require('jquery');<% } %><%if (jsLibs && jsLibs.indexOf('backbone') !== -1) { %>
var Backbone = require('backbone');<% } %><%if (jsLibs && jsLibs.indexOf('exoskeleton') !== -1) { %>
var Exo = require('exoskeleton');<% } %>

export default (function(){
	'use strict';

	// Save a reference to the global object
	var root = window;

	var App = root.App;
	<%if (jsLibs && jsLibs.indexOf('jquery') !== -1) { %>
	App.$ = $;<% } %>

	// Versioning
	App.version = "0.0.1";

	// Feature detection
	App.support = App.support || {};
	App.support.touch = 'ontouchstart' in window;

	return App;

}).call(this);