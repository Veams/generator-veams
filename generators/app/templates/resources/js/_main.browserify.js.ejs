// Global dependencies<% if (veamsPackages) { %>
import {App, Veams} from './app';<% } else { %>
import App from './app';<% } %>

console.log('JS initialized in version:', App.version);<% if (veamsPackages) { %>

// Imports
// @INSERTPOINT :: @ref: js-self-contained-import, @keep: true //

// Initialize modules with Veams
Veams.modules.register([
    // @INSERTPOINT :: @ref: js-init-v5, @keep: true //
]);<% } %>