require.config({
    paths: {<% if (jsLibs && jsLibs.length) { %> <% jsLibs.forEach(function(lib, i) {%><% if(lib === 'veams-query') { %>
		'<%= lib %>': '../bower-components/veams-query/lib/veams-query'<% if(i < (jsLibs.length - 1 )) { %>,<% } %><% } %><% if(lib === 'jquery') { %>
		'<%= lib %>': '../bower-components/jquery/dist/jquery'<% if(i < (jsLibs.length - 1 )) { %>,<% } %><% } %><% if(lib === 'backbone') { %>
		'<%= lib %>': '../bower-components/backbone/backbone'<% if(i < (jsLibs.length - 1 )) { %>,<% } %><% } %><% if(lib === 'exoskeleton') { %>
		'<%= lib %>': '../bower-components/exoskeleton/exoskeleton'<% if(i < (jsLibs.length - 1 )) { %>,<% } %><% } %> <% }); } %>
	},
	shim: {<% if (jsLibs && jsLibs.length) { if (jsLibs.indexOf('backbone') != -1) { %>
		'backbone': {
			deps: ['../bower-components/underscore/underscore', 'jquery'],
			exports: 'Backbone'
		}<% }  if (jsLibs.indexOf('exoskeleton') != -1) { %>
		'exoskeleton': {
			deps: ['../bower-components/underscore/underscore', 'jquery'],
			exports: 'Exoskeleton'
		}<% }} %>
	}
});

require([
	'app'<% if (jsLibs && jsLibs.length) { %> <% jsLibs.forEach(function(lib, i) {%>
	'<%= lib %>'<% if(i < (jsLibs.length - 1 )) { %>,<% } %><% }); } %>
	], function (
		app
	) {
    'use strict';

	console.log('JS initialized in version:', App.version);
});