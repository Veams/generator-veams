/* ===================================================
Main File For Imports
=================================================== */
<% if (cssLibs && cssLibs.length) { if (cssLibs.indexOf('neat') != -1) { %>
// Helpers
@import "../../../node_modules/bourbon/app/assets/stylesheets/bourbon";<% }} %>
<% if (veamsPackages) { %>
// Reset (veams-reset or veams-normalize)
@import "../../../node_modules/veams/src/scss/veams-reset";<% } %>

// Global Variables and Settings
@import "global/_vars";
@import "helpers/helpers";<% if (veamsPackages) { %>
@import "../../../node_modules/veams/src/scss/veams";<% } %><% if (cssLibs && cssLibs.length) { %>

// Frameworks <% if (cssLibs.indexOf('include-media') != -1) { %>
@import "../../../node_modules/include-media/dist/include-media";<% } %><% if (cssLibs.indexOf('foundation-sites') != -1) { %>
@import "../../../node_modules/foundation-sites/scss/foundation"; // You can choose your specific components // You can choose your specific components
@include foundation-everything;<% } %>
<% if (cssLibs.indexOf('neat') != -1) { %>
@import "../../../node_modules/neat/core/neat";<% } %> <% if (cssLibs.indexOf('bootstrap-sass') != -1) { %>
@import "../../../node_modules/bootstrap-sass/assets/stylesheets/bootstrap"; // You can choose your specific components<% }} %>

// Icons
@import "icons/**/*";

// Globals
@import "global/_base";<% if (veamsPackages) { %>
@import "global/_get-media";

// Regions in Layouts
@import "layouts/**/*";
<% } %>

// @INSERTPOINT :: @ref: scss-import, @keep: true //

// Print
@import "global/_print";