/* ===================================================
Main File For Imports
=================================================== */
<% if (veamsPackages) { %>
// Reset (veams-reset or veams-normalize)
@import "../../../node_modules/veams/src/scss/veams-reset";<% } %>

// Global Variables and Settings
@import "../../shared/styles/shared";<% if (cssLibs && cssLibs.length) { %>

// Frameworks<% if (cssLibs.indexOf('foundation-sites') != -1) { %>
@import "../../../node_modules/foundation-sites/scss/foundation"; // You can choose your specific components // You can choose your specific components
@include foundation-everything;<% } %>
<% if (cssLibs.indexOf('bootstrap-sass') != -1) { %>
@import "../../../node_modules/bootstrap-sass/assets/stylesheets/bootstrap"; // You can choose your specific components<% }} %>

// Icons
@import "../../shared/styles/icons/**/*";

// Globals
@import "app";<% if (veamsPackages) { %>
@import "../../shared/styles/global/_get-media";

// Regions in Layouts
@import "layouts/**/*";
<% } %>

// @INSERTPOINT :: @ref: scss-import, @keep: true //

// Print
@import "print";