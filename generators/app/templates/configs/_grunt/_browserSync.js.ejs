module.exports = {
	options: {
		notify: false,
		host: "localhost",
		proxy: 'localhost:<%%= ports.app %>',
		watchTask: true,
		ghostMode: {
			clicks: true,
			scroll: true,
			forms: true
		}
	},
	bsFiles: {
		src: [
			'<%%= paths.app %>/css/styles.css', <% if (docs && docs.length > 0 && docs.indexOf('htmldoc') != -1) { %>
			'<%%= paths.app %>/css/docs.css', <% } %>
			'<%%= paths.app %>/scripts/**/*.js',
			'<%%= paths.app %>/**/*.html'
		]
	}
};