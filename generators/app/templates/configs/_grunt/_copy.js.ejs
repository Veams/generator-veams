module.exports = { <% if (features && features.length) { %><% if (features.indexOf('createDevFolder') != -1){ %>
	dist: {
		cwd: '<%%= paths.app %>/',
		dest: '<%%= paths.dist %>',
		expand: true,
		src: ['**']
	}<% } %><% } %>
};