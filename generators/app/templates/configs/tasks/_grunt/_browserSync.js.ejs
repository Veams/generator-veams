module.exports = {
	options: {
		notify: false,
		host: "localhost",
		proxy: 'localhost:<%%= ports.app %>',
		watchTask: true,
		ghostMode: {
			clicks: true,
			scroll: true,
			forms: true
		}
	},
	bsFiles: {
		src: [
			'<%%= paths.dest %>/css/app.bundle.css',
			'<%%= paths.dest %>/css/docs.bundle.css',
			'<%%= paths.dest %>/scripts/**/*.js',
			'<%%= paths.dest %>/**/*.html'
		]
	}
};