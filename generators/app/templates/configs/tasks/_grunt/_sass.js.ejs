const magicImporter = require('node-sass-magic-importer');

module.exports = {
	options: {
		outputStyle: 'nested',
		sourceMap: true,
		importer: magicImporter()
	},
	dev: {
		files: {
			'<%%= paths.dest %>/css/app.bundle.css': '<%%= paths.app %>/app.scss'
		}
	},

	docs: {
		files: {
			'<%%= paths.dest %>/css/docs.bundle.css': '<%%= paths.app %>/docs/layouts/lyt-docs.scss'
		}
	}
};