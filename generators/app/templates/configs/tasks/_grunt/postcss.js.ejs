module.exports = {
	options: {
    log: true,
    processors: [<% if (cssLibs && cssLibs.length && cssLibs.indexOf('lost-grid') != -1) {%>
		require('lost')<% if (gruntModules && gruntModules.length && gruntModules.indexOf('grunt-autoprefixer') != -1) { %>,<% } %><% } %><% if (gruntModules && gruntModules.length && gruntModules.indexOf('grunt-autoprefixer') != -1) { %>
		require('autoprefixer')({
			browsers: ['last 2 versions', 'ie >= 10']
     	})<% } %>
    ]
  },
	dev: {
		options: {
			map: true
		},
		src: '<%%= paths.app %>/css/app.bundle.css'
	},
	dist: {
		options: {
			map: false
		},
		src: '<%%= paths.app %>/css/app.bundle.css'
	}
};