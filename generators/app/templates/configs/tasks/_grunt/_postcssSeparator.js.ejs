module.exports = {<% if (gruntModules && gruntModules.length && gruntModules.indexOf('grunt-grunticon') != -1) { %>
	icons: {
		options: {
			dataFile: true,
			pattern: {
				matchValue: /data/, // The RegExp to match values with
				matchRule: false, // The RegExp to match rules with
				matchParent: true // Rules (eg. in @media blocks) include their parent node.
			}
		},
		files: {
			'<%%= paths.app %>/css/styles.icons.css': ['<%%= paths.app %>/css/styles.css']
		}
	}<% } %>
};