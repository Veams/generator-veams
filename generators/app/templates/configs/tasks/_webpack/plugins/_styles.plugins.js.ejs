const ExtractTextPlugin = require('extract-text-webpack-plugin');<% if(testAndQALibs.indexOf('stylelint') !== -1 ) { %>
const StyleLintPlugin = require('stylelint-webpack-plugin');<% } %>

module.exports = function (configContext) {
	return [<% if(testAndQALibs.indexOf('stylelint') !== -1 ) { %>
		new StyleLintPlugin({
			configFile: `${configContext}/tasks/linting/stylelint.config.js`,
			files: [
				'**/*.s?(a|c)ss'
			]
		}),<% } %>
		new ExtractTextPlugin({
			filename: '/css/[name].bundle.css'
		})
	]
};