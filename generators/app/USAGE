Description:
    Creates a default Prototype app

Options:
    --skip-install: Skips the automatic execution of `bower` and `npm`
      after scaffolding has finished.
    --s: alias --skip-install

Example:
    yo prototype [--skip-install] [--silent]
