## INSERTPOINTS

### Include: Page

``` hbs
{{! @INSERT :: START @id: <%= filename %>, @tag: <%= customTypeName %>-partial }}
{{#with <%= filename %>}}<% if (props.bpWrapWith) { %>
	{{#wrapWith "<%= filename %>"}}
		Wrapped with markup from <%= filename %>.
	{{/wrapWith}}<% } else { %>
	{{> <%= filename %>}}<% } %>
{{/with}}
{{! @INSERT :: END }}
```
<% if(props.bpWithJs) {%>
### Include: JavaScript

#### Initializing in Veams V5
``` js
// @INSERT :: START @tag: js-init-v5 //
Veams.modules.add('<%= filename %>', <%= bpJsName %>);
// @INSERT :: END
```

#### Custom Events
``` js
// @INSERT :: START @tag: js-events //
/**
 * Events for <%= bpJsName %>
 */
EVENTS.<%= bpName %> = {
	eventName: '<%= bpName %>:eventName'
};
// @INSERT :: END
```
<% } %>