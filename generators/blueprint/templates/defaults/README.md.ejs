<%
let bpWithJs = props.blueprints.indexOf('/scripts/bp.js.ejs') !== -1;
%>
# <%= bpName %> (`<%= bpTypeName %>`)

## Description

This blueprint is based on the blueprint of Veams.

> Just place a generic quote for your type.

-----------

## Requirements<% if(bpWithJs) { %>
- [@veams/core](https://github.com/Veams/core) - Veams Core Framework.
- [@veams/component](https://github.com/Veams/component) - Veams Component Base Class.<% } %>

-----------

## Installation

### Installation with Veams from local machine

``` cmd
veams install bp absolute/filepath/to/<%= filename %>
```

### Installation with npm or Veams

When published on npm you can install the <%= bpTypeName %> by executing:

``` cmd
veams install c veams-<%= bpTypeName %> <%= filename %>
```

-----------

## Fields

### `<%= bpTypePrefix %><%= filename %>`
<% if (props.bpWithWrapWith) { %>
The partial is a `wrapWith` partial. This means that you can access all properties and hashes via `props`.
See [Mangony Handlebars Helper - wrapWith](https://www.npmjs.com/package/mangony-hbs-helper-wrap-with) for detailed instructions.
<% } %>

| Parameter | Type | Value | Description |
|:--- | :---: |:---: | :--- |
| settings.<%= bpName %>Classes | String | | _Modifier classes for <%= bpTypeName %>._ | <% if (bpTypeName === 'component') { %>
| settings.<%= bpName %>ContextClass | String | `default` |  - _Context class of component._ | <% } %> <% if(bpWithJs) {%>
| settings.<%= bpName %>JsOptions | Object | | _JavaScript options which gets stringified._ | <% } %>

#### Content
| Parameter | Type | Value | Description |
|:--- | :---: |:---: | :--- |
| content.<%= bpName %>Field | String | |  _Please add a description!_ |
<% if(bpWithJs) { %>
-------------

## JavaScript Options

The module gives you the possibility to override default JS options:

| Option | Type | Default | Description |
|:--- |:---:|:---:|:--- |
| optionOne | String | `is-option` | _Please add a description!_ |
<% } %>
------------

## Sass Options

| Variable | Value | Description |
|:--- | :---: |:--- |
| $<%= filename %>-my-custom-var | | _Please add a description!_ |
