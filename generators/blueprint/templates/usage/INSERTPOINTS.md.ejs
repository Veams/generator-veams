## INSERTPOINTS

### Include: Page

``` hbs
{{! @INSERT :: START @id: <%= filename %>, @tag: <%= bpTypeName %>-partial }}
{{#with <%= filename %>-bp}}<% if (bpWrapWith) { %>
	{{#wrapWith "<%= bpTypePrefix %><%= filename %>"}}
		Wrapped with markup from <%= filename %>.
	{{/wrapWith}}<% } else { %>
	{{> <%= bpTypePrefix %><%= filename %>}}<% } %>
{{/with}}
{{! @INSERT :: END }}
```
<% if(bpWithJs) {%>
### Include: JavaScript

#### Initializing in Veams V5
``` js
// @INSERT :: START @tag: js-init-v5 //
// Init <%= bpJsName %>
Veams.modules.add('<%= filename %>', <%= bpJsName %>);
// @INSERT :: END
```

#### Custom Events
``` js
// @INSERT :: START @tag: js-events //
/**
 * Events for <%= bpJsName %>
 */
EVENTS.<%= bpName %> = {
	eventName: '<%= bpName %>:eventName'
};
// @INSERT :: END
```
<% } %>