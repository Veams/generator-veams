# <%= filename %>

This blueprint is based on the blueprint of Veams-Components.

## Usage

### Include: Page

``` hbs
{{#with <%= filename %>-bp}}
	{{> c-<%= filename %>}}
{{/with}}
```

### Include: SCSS

``` scss
@import "blueprints/_c-<%= filename %>";
```
<% if(blueprintWithJs) {%>
### Include: JavaScript

#### Import
``` js
import <%= blueprintJsName %> from './modules/<%= filename %>/<%= filename %>';
```

#### Initializing
``` js
/**
 * Init <%= blueprintJsName %>
 */
Helpers.loadModule({
	el: '[data-js-module="<%= filename %>"]',
	module: <%= blueprintJsName %>,
	context: context
});
```
<% } %>