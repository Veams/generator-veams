# <%= filename %>

This blueprint is based on the blueprint of Veams-Components.

## Usage

### Include: Page

``` hbs
{{! @INSERT :: START @id: component, @tag: <% if (bpType === 'c-') { %>component<% } else if (bpType === 'b-') { %>block<% } else { %>global<% } %>
{{#with <%= filename %>-bp}}
	{{> <%= bpType %><%= filename %>}}
{{/with}}
{{! @INSERT :: END }}
```

### Include: SCSS

``` scss
// @INSERT :: START @id: scss-import, @tag: <% if (bpType === 'c-') { %>component<% } else if (bpType === 'b-') { %>block<% } else { %>global<% } %>
@import "<% if (bpType === 'c-') { %>components<% } else if (bpType === 'b-') { %>blocks<% } else { %>globals<% } %>/_<%= bpType %><%= filename %>";
// @INSERT :: END
```
<% if(bpWithJs) {%>
### Include: JavaScript

#### Import
``` js
// @INSERT :: START @id: js-import, @tag: <% if (bpType === 'c-') { %>component<% } else if (bpType === 'b-') { %>block<% } else { %>global<% } %>
import <%= bpJsName %> from './modules/<%= filename %>/<%= filename %>';
// @INSERT :: END
```

#### Initializing in Veams V2
``` js
// @INSERT :: START @id: js-init-v2, @tag: <% if (bpType === 'c-') { %>component<% } else if (bpType === 'b-') { %>block<% } else { %>global<% } %>
/**
 * Init <%= bpJsName %>
 */
Helpers.loadModule({
	el: '[data-js-module="<%= filename %>"]',
	module: <%= bpJsName %>,
	context: context
});
// @INSERT :: END
```

#### Initializing in Veams V3
``` js
// @INSERT :: START @id: js-init-v3, @tag: <% if (bpType === 'c-') { %>component<% } else if (bpType === 'b-') { %>block<% } else { %>global<% } %>
/**
 * Init <%= bpJsName %>
 */
Helpers.loadModule({
	domName: '<%= filename %>',
	module: <%= bpJsName %>,
	context: context
});
// @INSERT :: END
```
<% } %>